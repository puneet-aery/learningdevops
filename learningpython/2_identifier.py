# name given to a memory location is called an identifier. It is used to identify a variable, function, class, module or other object in Python. An identifier starts with a letter (A-Z or a-z) or an underscore (_) followed by zero or more letters, underscores and digits (0-9). Python does not allow special characters like @, $, and % within identifiers.
a=1 # a is an integer variable
b=2.2 # b is a float variable
c="Hello" # c is a string variable
d=True # d is a boolean variable